#include <iostream>

using namespace std;

int main()
{
    int i, a, b, c, d, k=0;
    cin>>a>>b;
    cin>>c>>d;
    for (i=10000; i<100000; i++) {
        if ((i % a == b) && (i % c == d)) {
            cout<<i<<" ";
            k++;
        }
    }
    if (k==0) {
        cout<<-1;
    }
    return 0;
}
